#include <iostream>
using namespace std;

// Write a program to read two space-separated integers in a single input statement and print them
int main() {
    int a, b;
    cout << "Enter two integers separated by space: ";
    cin >> a >> b;
    cout << "First: " << a << ", Second: " << b << endl;
    return 0;
}